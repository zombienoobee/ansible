- hosts: all
  become: true
  become_user: root
  tasks:
  - name: Remove a line from the file
    lineinfile:
      path: /etc/sudoers
      state: absent
      regexp: "{{ item }} "
    loop:
      - "^rssansbl"
      - "^nrpe"
      - "^%admins"
      backup: yes
      validate: 'visudo -cf %s'
# - name: replace a comment in sudoer file
#    replace:
#     path: /etc/sudoers
#      regexp: '
